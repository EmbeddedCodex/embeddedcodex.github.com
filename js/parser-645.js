// 645 协议解析函数
function parseHex_645() {
    const input = document.getElementById('hexInput_645').value;
    const resultDiv = document.getElementById('result_645');

    if (!input) {
        resultDiv.innerHTML = "请输入645协议的十六进制字符串";
        return;
    }

    // 这里可以添加645协议的解析逻辑
    resultDiv.innerHTML = "645协议解析功能待实现";
}

// export { parseHex_645 };
